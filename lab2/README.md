# Лабораторная работа 2

Условие:

Создание предикатов для обработки списков.

Вариант 6:
Слить два упорядоченных списка в один, сохранив упорядоченность.

Файл с программой: solution.pl

Запрос:
merge([1, 3, 5], [2, 4, 6], X).

Результат:
X = [1, 2, 3, 4, 5, 6].

Запрос:
merge([1, 2, 7], [3, 4, 5], X).

Результат:
X = [1, 2, 3, 4, 5, 7].

Запрос:
merge([], [2, 4, 6], X).

Результат:
X = [2, 4, 6].

Запрос:
merge([1, 3, 5], [2, 4, 6], [1, 2, 3, 4, 5, 6]).

Результат:
true.
